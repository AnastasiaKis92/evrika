.page-subsection
  h1 + &
  h2 + &
    margin-top 3rem
  &--partners-subsection
    h2 + &
      margin-top 1rem
  &--order
    padding 3rem 6rem
    box-shadow var(--shadow)
    @media (max-width point-m)
      padding 2rem 3rem
  &--padding-y
    padding 4.6rem 0

  @media (max-width point-m)
    h1 + &
    h2 + &
      margin-top 2rem
    &--padding-y
      padding 2rem 0
